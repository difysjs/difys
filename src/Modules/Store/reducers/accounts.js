import {
	/* account */
	addAccount,
	initAccount,
	setAccountId,
	setStatus,
	/* auth */
	setHaapi,
	setToken,
	/* map */
	setCoords,
	pushEntity,
	removeEntity,
	/* inventory */
	addItem,
	removeItem,
	setEquipment
} from "../actions";

const accountsReducers = {
	/* account */
	addAccount,
	initAccount,
	setAccountId,
	setStatus,
	/* auth */
	setHaapi,
	setToken,
	/* map */
	setCoords,
	pushEntity,
	removeEntity,
	/* inventory */
	addItem,
	removeItem,
	setEquipment
};

export default accountsReducers;
