// https://difysjs.github.io/introduction/configuration#accounts

const accountsList = {
	/* yourUsername: {
		username: "yourUsername",
		password: "yourPassword",
		directLogin: false, if true, everything after this is ignored (check the docs)
		server: "Oshimo",
		character: "myCharacter"
		proxy: {
			type: "socks", // https OR http OR socks (works both for v5 or v4 )
			ip: "127.0.0.1:80" // WITH PORT
			login: "", // remove this if your proxy doesnt support authentification
			password: ""
		}
	} */
};

export default accountsList;
